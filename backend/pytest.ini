[pytest]
testpaths = tests
markers =
    integration: marks tests as integration (run against real Postgres)
    concurrency: marks tests as concurrency/race-condition tests
    security: marks tests as security authorization and exposure tests
addopts = -v --tb=short
asyncio_mode = auto
