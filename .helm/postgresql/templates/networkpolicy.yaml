{{- if .Values.networkPolicy }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: offlinepos-postgresql-policy
  namespace: {{ .Values.namespace }}
spec:
  podSelector:
    matchLabels:
      app: offlinepos-postgresql
  policyTypes:
    - Ingress
  ingress:
    {{- range .Values.networkPolicy.allowedFrom }}
    - from:
        - podSelector:
            matchLabels:
              app: {{ .app }}
      ports:
        - protocol: TCP
          port: {{ $.Values.service.port }}
    {{- end }}
{{- end }}
