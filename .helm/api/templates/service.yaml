apiVersion: v1
kind: Service
metadata:
  name: offlinepos-api-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: offlinepos-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: offlinepos-api
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
